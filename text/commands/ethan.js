module.exports = {
    name: 'ethan',
    secret: true,
    spam: false,
    description: "ethan lmao",
    execute(message, args, bot)
    {
        if (message.author.id === bot.jaspaID)
        {
         
        }
    }
}  