const { MessageFlags } = require("discord.js")

module.exports = {
    id: '321665327845081089',
    name: 'matt',
    secret: false,
    description: "",
    execute(message, bot)
    {
        if (false)
        {
            message.delete();
        }
    }
}  