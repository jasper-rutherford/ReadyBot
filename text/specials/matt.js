const { MessageFlags } = require("discord.js")

module.exports = {
    id: '111579235059060736',
    name: 'matt',
    secret: false,
    description: "",
    execute(message, bot)
    {
        if (true)
        {
            message.delete();
        }
    }
}  