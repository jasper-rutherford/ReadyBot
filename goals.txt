lets do one endpoint, and tests for the endpoint. do ci/cd. make the server defined somewhere, and start it elsewhere. then tests can import the server and do requests at it in code. and run a postgres db in the ci/cd too. something small, for tests. and then i guess maybe have all the tests stuff in its own docker compose or something?
